<div class="row mt-5">
  <div class="col-md-6 m-auto">
    <div class="card card-body">
      <h1 class="text-center mb-3">
        <i class="fas fa-user-plus"></i> Register
      </h1>
      <% include ./partials/messages %>
      <form action="/users/register" method="POST">
        <div class="form-group">
          <label for="name">Name</label>
          <input
            type="name"
            id="name"
            name="name"
            class="form-control"
            placeholder="Enter Name"
            value="<%= typeof name != 'undefined' ? name : '' %>"
          />
        </div>


        <div class="form-group">
          <label for="email">Email</label>
          <input
            type="email"
            id="email"
            name="email"
            class="form-control"
            placeholder="Enter Email"
            value="<%= typeof email != 'undefined' ? email : '' %>"
          />
        </div>


      <div class="form-group">
          <label for="email">Vehical No</label>
          <input
            type="name"
            id="vno"
            name="vno"
            class="form-control"
            placeholder="Enter Vehical No"
            value="<%= typeof vno != 'undefined' ? email : '' %>"
          />
        </div>


        <div class="form-group">
          <label for="email">Tag ID</label>
          <input
            type="name"
            id="tagid"
            name="tagid"
            class="form-control"
            placeholder="Enter Tag ID"
            value="<%= typeof tagid != 'undefined' ? email : '' %>"
          />
        </div>

        <div class="form-group">
          <label for="ctype">Choose vehicle type:</label>
          <select
            id="ctype"
            name="ctype"
            class="form-control"
            placeholder="Select Vehicle Type"
          />

<option value="40">Car</option>
  <option value="50">Jeep</option>
  <option value="100">Bus</option>
  <option value="200">Truck</option>
</select>

        </div>





        <div class="form-group">
          <label for="password">Password</label>
          <input
            type="password"
            id="password"
            name="password"
            class="form-control"
            placeholder="Create Password"
            value="<%= typeof password != 'undefined' ? password : '' %>"
          />
        </div>


        <div class="form-group">
          <label for="password2">Confirm Password</label>
          <input
            type="password"
            id="password2"
            name="password2"
            class="form-control"
            placeholder="Confirm Password"
            value="<%= typeof password2 != 'undefined' ? password2 : '' %>"
          />
        </div>
        <button type="submit" class="btn btn-primary btn-block">
          Register
        </button>
      </form>
      <p class="lead mt-4">Have An Account? <a href="/users/login">Login</a></p>
    </div>
  </div>
</div>
